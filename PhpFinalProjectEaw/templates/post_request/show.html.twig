{% extends 'base.html.twig' %}

{% block title %}PostRequest{% endblock %}

{% block body %}
    <h1>PostRequest</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post_request.id }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ post_request.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('post_request_index') }}">back to list</a>

    <a href="{{ path('post_request_edit', {'id': post_request.id}) }}">edit</a>

    {{ include('post_request/_delete_form.html.twig') }}
{% endblock %}
